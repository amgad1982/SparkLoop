# ==============================================================================
# Coturn TURN/STUN Server Docker Compose for Oracle Cloud (ARM64)
# Server IP: 92.4.162.183
# Web Admin Port: 8080 (HTTPS)
# ==============================================================================

services:
  coturn:
    image: coturn/coturn:latest
    container_name: sparkloop-coturn
    restart: always
    # network_mode: host is essential for high-performance WebRTC UDP relaying
    # Avoids docker-proxy overhead and NAT latency for thousands of UDP packets
    network_mode: host
    volumes:
      - ./turnserver.conf:/etc/coturn/turnserver.conf:ro
      - ./certs:/etc/coturn/certs:ro
      - ./data:/var/lib/turn
    command: ["-c", "/etc/coturn/turnserver.conf"]
